# Railway 暴露服务获取 URL

## 🚀 获取公共 URL

你的服务已经成功部署，但需要暴露到公网才能访问。

### 步骤：

1. **进入 Railway 项目**
   - 访问 https://railway.app
   - 选择你的 "Personalcz" 项目

2. **生成公共域名**
   - 在服务页面，找到 "Settings" 标签
   - 或者直接在服务卡片上找到 "Generate Domain" 按钮
   - 点击 "Generate Domain" 或 "Settings" → "Networking"

3. **配置端口**
   - 确保端口设置为 `3000`（你的 server.js 使用的端口）
   - Railway 会自动检测，但确认一下

4. **获取 URL**
   - Railway 会生成一个类似这样的 URL：
     - `https://personalcz-production.up.railway.app`
     - 或 `https://personalcz-xxxx.up.railway.app`

5. **访问网站**
   - 复制生成的 URL
   - 在浏览器中打开即可访问

## 📝 详细步骤（图文）

### 方法一：通过 Settings

1. 点击服务名称 "Personalcz"
2. 点击 "Settings" 标签
3. 找到 "Networking" 部分
4. 点击 "Generate Domain" 按钮
5. Railway 会自动生成一个 `.up.railway.app` 域名

### 方法二：直接在服务卡片

1. 在服务卡片上找到 "..." 菜单
2. 选择 "Generate Domain"
3. 或直接点击服务卡片上的 "Generate Domain" 按钮

## ⚙️ 端口配置

确保端口配置正确：
- **Port**: `3000`（你的 server.js 使用的端口）
- Railway 通常会自动检测，但可以手动设置

## 🔒 自定义域名（可选）

如果你有自己的域名：
1. 在 Settings → Networking
2. 添加自定义域名
3. 配置 DNS 记录

## ✅ 完成

生成域名后，你就可以通过该 URL 访问你的网站了！



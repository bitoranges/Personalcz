# 部署指南 - 最快最简单的方法

## 🚀 方法一：Railway（推荐，最简单）

### 步骤：

1. **注册 Railway 账号**
   - 访问 https://railway.app
   - 使用 GitHub 账号登录（最简单）

2. **连接 GitHub 仓库**
   - 在 Railway 点击 "New Project"
   - 选择 "Deploy from GitHub repo"
   - 选择你的仓库

3. **自动部署**
   - Railway 会自动检测 Node.js 项目
   - 自动运行 `npm install` 和构建

4. **配置环境变量**
   在 Railway 项目设置中添加以下环境变量：
   ```
   PORT=3000
   NODE_ENV=production
   NETWORK=base-mainnet
   BASE_RPC_URL=https://mainnet.base.org
   RECEIVER_ADDRESS=你的钱包地址
   ```

5. **完成！**
   - Railway 会自动分配一个 URL（如：`your-project.railway.app`）
   - 访问该 URL 即可使用

### 优点：
- ✅ 零配置，自动检测
- ✅ 免费层可用（每月 $5 额度）
- ✅ 自动 HTTPS
- ✅ 自动部署（Git push 即部署）

---

## 🌐 方法二：Render（同样简单）

### 步骤：

1. **注册 Render 账号**
   - 访问 https://render.com
   - 使用 GitHub 账号登录

2. **创建 Web Service**
   - 点击 "New +" → "Web Service"
   - 连接你的 GitHub 仓库

3. **配置设置**
   - **Name**: 你的项目名称
   - **Environment**: Node
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
   - **Root Directory**: `/` (根目录)

4. **添加环境变量**
   在 Environment Variables 中添加：
   ```
   PORT=3000
   NODE_ENV=production
   NETWORK=base-mainnet
   BASE_RPC_URL=https://mainnet.base.org
   RECEIVER_ADDRESS=你的钱包地址
   ```

5. **部署**
   - 点击 "Create Web Service"
   - 等待构建完成

### 优点：
- ✅ 免费层可用
- ✅ 自动 HTTPS
- ✅ 简单易用

---

## 📝 部署前检查清单

### 1. 确保前端已构建
```bash
npm run build
```
这会生成 `frontend/dist` 目录

### 2. 检查 `.env` 文件
确保你有正确的环境变量（但不要提交到 Git）

### 3. 确保 `server.js` 可以服务静态文件
✅ 已配置好，无需修改

### 4. 测试本地生产环境
```bash
# 构建前端
npm run build

# 启动服务器
npm start

# 访问 http://localhost:3000
```

---

## 🔧 如果使用 Railway（推荐）

Railway 会自动：
- 检测 `package.json`
- 运行 `npm install`
- 运行构建命令（如果有）
- 启动 `npm start`

**你只需要：**
1. 连接 GitHub
2. 添加环境变量
3. 完成！

---

## 🎯 最快部署流程（Railway）

```bash
# 1. 确保代码已提交到 GitHub
git add .
git commit -m "Ready for deployment v0.1"
git push

# 2. 在 Railway 中：
#    - 点击 "New Project"
#    - 选择你的 GitHub 仓库
#    - 添加环境变量
#    - 等待部署完成（约 2-3 分钟）

# 3. 访问 Railway 提供的 URL
```

---

## ⚠️ 注意事项

1. **环境变量安全**
   - 不要将 `.env` 文件提交到 Git
   - 在部署平台添加环境变量

2. **构建输出**
   - 确保 `frontend/dist` 目录在 `.gitignore` 中
   - Railway/Render 会自动构建

3. **端口配置**
   - Railway/Render 会自动设置 `PORT` 环境变量
   - 你的代码已经使用 `process.env.PORT || 3000`，所以没问题

4. **静态文件路径**
   - `server.js` 已配置好服务 `frontend/dist`
   - 无需额外配置

---

## 🆘 遇到问题？

### 构建失败
- 检查 `package.json` 中的构建脚本
- 确保所有依赖都已安装

### 环境变量未生效
- 在部署平台重新添加环境变量
- 重启服务

### 静态文件 404
- 确保 `npm run build` 成功生成 `frontend/dist`
- 检查 `server.js` 中的静态文件路径

---

## 📞 推荐顺序

1. **首选：Railway** - 最简单，零配置
2. **备选：Render** - 同样简单，免费层稳定
3. **其他：Vercel/Netlify** - 需要更多配置

**建议：直接使用 Railway，5 分钟即可完成部署！**
